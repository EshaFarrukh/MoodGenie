{
  "name": "moodgenie-firebase-rules-tests",
  "private": true,
  "type": "commonjs",
  "scripts": {
    "test:runner": "node --test firestore.rules.test.js storage.rules.test.js",
    "test": "firebase emulators:exec --project moodgenie-rules --only firestore,storage \"npm run test:runner\""
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^4.0.1",
    "firebase": "^11.0.0",
    "firebase-tools": "^14.25.0"
  }
}
